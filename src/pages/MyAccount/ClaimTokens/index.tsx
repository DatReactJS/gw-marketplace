import React from 'react';
import styles from './index.less';

interface Props {}

const ClaimTokens: React.FC<Props> = (props: Props) => {
  return <div className={styles.claimTokens}>ClaimTokens</div>;
};

export default ClaimTokens;
